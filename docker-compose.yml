version: "0.1"

services:
  trainer:
    build:
      context: .
    profiles:
      - "dev"
    image: "spira-training"
    entrypoint: "python training-pipeline/model_pipeline.py"
    volumes:
      - ".:/app/"
    ports:
      - "3000:8000"
